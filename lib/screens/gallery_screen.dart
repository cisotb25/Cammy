import 'package:flutter/material.dart';

class GalleryScreen extends StatelessWidget { // Change this name for each file
  const GalleryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Gallery lives here")), // Change text
    );
  }
}