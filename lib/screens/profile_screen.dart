import 'package:flutter/material.dart';

class ProfileScreen extends StatelessWidget { // Change this name for each file
  const ProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Profile lives here")), // Change text
    );
  }
}