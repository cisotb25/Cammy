import 'package:flutter/material.dart';

class TrashScreen extends StatelessWidget { // Change this name for each file
  const TrashScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Trashy lives here")), // Change text
    );
  }
}